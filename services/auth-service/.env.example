# Copy to .env and fill in real values. Never commit .env.
# PostgreSQL connection string used by Prisma and the pg adapter.
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/flashtix_auth?schema=public"

# Port the auth service listens on. Defaults to 4001 when unset.
PORT=4001
